# Detect water quality data columns intelligently

Detect water quality data columns intelligently

## Usage

``` r
detect_water_quality_columns(
  water_sf,
  variable,
  date_column,
  station_id_col,
  verbose
)
```
