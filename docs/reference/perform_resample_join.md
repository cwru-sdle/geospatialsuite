# Perform resample join (raster to raster)

Perform resample join (raster to raster)

## Usage

``` r
perform_resample_join(
  source_info,
  target_info,
  scale_factor,
  summary_function,
  crs_target,
  verbose
)
```
