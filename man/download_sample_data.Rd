% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/16-data-download.R
\name{download_sample_data}
\alias{download_sample_data}
\title{Download or access sample datasets for geospatialsuite}
\usage{
download_sample_data(
  dataset = "all",
  dest_dir = tempdir(),
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{dataset}{Name of dataset to download. See \code{list_sample_datasets()}
for available options. Use "all" to list all datasets.}

\item{dest_dir}{Directory to save downloaded data (default: tempdir())}

\item{overwrite}{Overwrite existing files? (default: FALSE)}

\item{verbose}{Print download progress? (default: TRUE)}
}
\value{
Character vector of file paths to sample data
}
\description{
Access built-in sample datasets for learning and testing geospatialsuite
functionality. All datasets are in .rds format (CRAN-preferred) and
included with the package (~50-80KB total).
}
\details{
\subsection{Built-in Sample Data (No Download Required)}{

The package includes small sample datasets in inst/extdata/ using .rds format:
\itemize{
\item \strong{sample_red.rds}: Red band SpatRaster (10x10 pixels, Ohio region)
\item \strong{sample_nir.rds}: NIR band SpatRaster (10x10 pixels)
\item \strong{sample_blue.rds}: Blue band SpatRaster (10x10 pixels)
\item \strong{sample_green.rds}: Green band SpatRaster (10x10 pixels)
\item \strong{sample_swir1.rds}: SWIR1 band SpatRaster (10x10 pixels)
\item \strong{sample_multiband.rds}: Multi-band SpatRaster (5 layers stacked)
\item \strong{sample_points.rds}: Sample field locations (20 points, sf object)
\item \strong{sample_boundary.rds}: Sample study area polygon (sf object)
\item \strong{sample_coordinates.csv}: Sample coordinates with attributes
}

All .rds files contain native R objects (SpatRaster or sf) that load instantly
with readRDS() or the load_sample_data() helper function.
}
}
\examples{
\donttest{
# List available datasets
datasets <- list_sample_datasets()
print(datasets)

# Get path to built-in sample data
red_file <- get_sample_data("sample_red.rds")
nir_file <- get_sample_data("sample_nir.rds")

# Load and use sample data
red <- load_sample_data("sample_red.rds")
nir <- load_sample_data("sample_nir.rds")
ndvi <- calculate_vegetation_index(red = red, nir = nir, index_type = "NDVI")
  # Plot (users can run this interactively)
  # plot(ndvi, main = "NDVI", col = terrain.colors(100))
}

}
