# Perform cross-validation for interpolation accuracy

Perform cross-validation for interpolation accuracy

## Usage

``` r
perform_cross_validation(
  spatial_sf,
  target_variables,
  method,
  cv_folds,
  power,
  max_distance,
  min_points,
  max_points,
  verbose
)
```
